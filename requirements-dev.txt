# 로컬 개발용 OCR. 운영 이미지에는 넣지 않는다.
# paddlepaddle 만 500MB가 넘어 이미지가 4배로 커진다.
# 운영은 OCR_ENGINE=clova 또는 google 로 돌린다.
-r requirements.txt
paddleocr==2.9.1
paddlepaddle==2.6.2
opencv-python==4.11.0.86
pytest==8.*
