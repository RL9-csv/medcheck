.env
__pycache__/
.venv/

# 원본 수집물은 올리지 않는다. dur.db 하나로 재현 가능하다.
data/*.csv
data/*.json
data/*.xlsx
data/raw/

# dur.db(20MB)는 커밋한다. Dockerfile이 COPY 하므로 저장소에 없으면 빌드가 깨진다.
data/*.db
!data/dur.db

# OCR 모델은 저장소에 넣지 않는다. PADDLE_MODEL_DIR 로 지정한다.
models/
